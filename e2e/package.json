{
  "name": "ng-admin-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright acceptance suite. Behavioral baseline captured against the legacy AngularJS app; same specs must stay green through the React port.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
